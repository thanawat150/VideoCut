# Security Policy

Phase 1 เป็น Local-only ไม่ส่งวิดีโอออกจากเครื่อง ใช้ `ProcessStartInfo.ArgumentList` สำหรับ FFmpeg/FFprobe ปฏิเสธการ Export ทับ Source และไม่เก็บ API Key

ช่องโหว่ที่มีข้อมูลละเอียดควรแจ้งเจ้าของ Repository แบบส่วนตัว ไม่ควรเปิด Public Issue พร้อมข้อมูลที่นำไปโจมตีได้ทันที
