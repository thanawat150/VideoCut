<Window x:Class="AutoCutStudio.App.AutomaticEditingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Automatic Editing — Local Audio Analysis"
        Width="1040"
        Height="760"
        MinWidth="900"
        MinHeight="650"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="ตัดต่ออัตโนมัติจากเสียงจริง" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="วิเคราะห์ระดับเสียงด้วย FFmpeg ภายในเครื่อง สร้างแผนก่อนแก้ Timeline และไม่แก้ Source Media"
                       Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="คำสั่ง" FontWeight="SemiBold"/>
                <TextBox x:Name="CommandTextBox" Text="ตัดช่วงเงียบออก"/>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="ระดับการตัด" FontWeight="SemiBold"/>
                <ComboBox x:Name="PresetComboBox" SelectedIndex="1" Margin="4" Padding="7">
                    <ComboBoxItem Content="นุ่มนวล — เก็บจังหวะหายใจ" Tag="gentle"/>
                    <ComboBoxItem Content="สมดุล — แนะนำ" Tag="balanced"/>
                    <ComboBoxItem Content="กระชับ — ตัดไวขึ้น" Tag="aggressive"/>
                </ComboBox>
            </StackPanel>
            <Button x:Name="AnalyzeButton" Grid.Column="2" Content="วิเคราะห์ใหม่"
                    Click="Analyze_Click" VerticalAlignment="Bottom" Background="#0E7490"/>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <ProgressBar x:Name="AnalysisProgress" Height="10" IsIndeterminate="False"/>
                <TextBlock x:Name="AnalysisStatusText" Text="พร้อมวิเคราะห์" Margin="4,8,4,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Background="#0F172A" BorderBrush="#334155" BorderThickness="1"
                CornerRadius="6" Padding="10" Margin="0,0,0,10">
            <TextBlock x:Name="SummaryTextBlock" Text="ยังไม่มีแผนตัดต่อ" FontSize="16" FontWeight="SemiBold"
                       TextWrapping="Wrap"/>
        </Border>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.15*"/>
                <ColumnDefinition Width="1.85*"/>
            </Grid.ColumnDefinitions>
            <GroupBox Grid.Column="0" Header="ช่วงเงียบที่ตรวจพบ" Margin="0,0,8,0">
                <DataGrid x:Name="SilenceGrid" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="เริ่ม (s)" Binding="{Binding StartSeconds, StringFormat=0.000}" Width="*"/>
                        <DataGridTextColumn Header="จบ (s)" Binding="{Binding EndSeconds, StringFormat=0.000}" Width="*"/>
                        <DataGridTextColumn Header="ยาว (s)" Binding="{Binding DurationSeconds, StringFormat=0.000}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
            <GroupBox Grid.Column="1" Header="Timeline ที่เสนอให้เก็บ">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="110"/>
                    </Grid.RowDefinitions>
                    <DataGrid x:Name="ProposedSegmentsGrid" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Segment" Binding="{Binding Id}" Width="1.5*"/>
                            <DataGridTextColumn Header="เริ่ม (s)" Binding="{Binding StartSeconds, StringFormat=0.000}" Width="*"/>
                            <DataGridTextColumn Header="จบ (s)" Binding="{Binding EndSeconds, StringFormat=0.000}" Width="*"/>
                            <DataGridTextColumn Header="ยาว (s)" Binding="{Binding DurationSeconds, StringFormat=0.000}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <TextBox x:Name="WarningsTextBox" Grid.Row="1" IsReadOnly="True" AcceptsReturn="True"
                             TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                </Grid>
            </GroupBox>
        </Grid>

        <DockPanel Grid.Row="5" Margin="0,12,0,0">
            <TextBlock DockPanel.Dock="Left" Text="ระบบนี้ยังไม่ถอดเสียงและไม่ลบคำพูดผิด — ใช้การวิเคราะห์ระดับเสียงเท่านั้น"
                       Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center"/>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="ยกเลิก" Click="Cancel_Click"/>
                <Button x:Name="ApplyButton" Content="ใช้กับ Timeline" Click="Apply_Click" IsEnabled="False"/>
                <Button x:Name="ApplyAndExportButton" Content="ใช้และ Export อัตโนมัติ"
                        Click="ApplyAndExport_Click" IsEnabled="False" Background="#047857"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
